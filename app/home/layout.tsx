import type { Metadata } from "next";

import Header from "@/components/shared/dashboard-layout/header";
import SideNav from "@/components/shared/dashboard-layout/side-nav";
import PageWrapper from "@/components/shared/dashboard-layout/page-wrapper";
import HeaderMobile from "@/components/shared/dashboard-layout/header-mobile";
import MarginWidthWrapper from "@/components/shared/dashboard-layout/margin-width-wrapper";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex">
      <SideNav />
      <main className="flex-1">
        <MarginWidthWrapper>
          <Header />
          <HeaderMobile />

          <PageWrapper>{children}</PageWrapper>
        </MarginWidthWrapper>
      </main>
    </div>
  );
}
